version: "3.7"
services:
  php:
    build:
      context: ..
      dockerfile: .docker/Dockerfile
      target: dev
    container_name: php
    volumes:
      - monitor_home:/home/zalex
      - "..:/usr/src/app"
      - ~/.composer:/home/zalex/.composer
volumes:
  monitor_home:
